{
  "name": "erasezo-web",
  "version": "0.1.0",
  "private": true,
  "description": "Erasezo dashboard + API \u2014 Node/Express + PostgreSQL (Railway).",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "for f in test/*.test.js; do node \"$f\" || exit 1; done && echo \"\\nall suites passed\"",
    "sync": "sh sync-design.sh && sh sync-ext.sh"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
